#ifndef NAMELESSGUI_COLORS_HPP
#define NAMELESSGUI_COLORS_HPP

#include <SFML/Graphics/Color.hpp>

const sf::Color DEFAULT_FILL_COLOR(40, 40, 40, 255);
const sf::Color DEFAULT_OUTLINE_COLOR(120, 120, 120);
const sf::Color ERROR_OUTLINE_COLOR(sf::Color::Red);

const sf::Color BUTTON_DEFAULT_FILL_COLOR(60, 60, 60, 255);
const sf::Color BUTTON_HOVER_FILL_COLOR(80, 80, 80, 255);
const sf::Color BUTTON_ACTIVE_FILL_COLOR(50, 50, 50, 255);
const sf::Color BUTTON_OUTLINE_COLOR(30, 30, 30, 255);

const sf::Color LINE_INPUT_CURSOR_COLOR(255, 255, 255);

const float DEFAULT_OUTLINE_THICKNESS(1);

#endif // NAMELSSGUI_COLORS_HPP
